# Disable optional dependencies to avoid Rollup errors
optional=false
# Avoid platform-specific binaries
omit=optional
# Force legacy peer dependencies to avoid compatibility issues
legacy-peer-deps=true
# Ensure consistent lockfile
package-lock=true
