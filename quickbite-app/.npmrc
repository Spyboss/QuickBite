# Enable optional dependencies to ensure Rollup platform-specific binaries are installed
optional=true
# Don't omit optional dependencies
omit=dev
# Force legacy peer dependencies to avoid compatibility issues
legacy-peer-deps=true
# Ensure consistent lockfile
package-lock=true
# Prefer offline packages for faster installs
prefer-offline=true
